@echo off
REM start_tunneled.bat - Launch the Corridor Monitor with a PUBLIC HTTPS URL.
REM Double-click before the demo; the public URL is printed when ready.
REM The URL changes on each launch; app is reachable while this machine is online.

bash "%~dp0start_tunneled.sh"
pause
